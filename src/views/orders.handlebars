<div class="container-home">
    {{> nav user=user admin=admin btnInicio=btnInicio}}
    <div class="container-update">
        <h3>Órdenes</h3>
        {{> pagination hasNextPage=hasNextPage hasPrevPage=hasPrevPage nextPage=nextPage prevPage=prevPage page=page }}
        <div class="div-prods-update">
            {{#each orders}}
            <div class="each-link-prod">
                <ul>
                    <li><a href="http://localhost:4020/show/order/{{this._id}}">Orden: {{this.created_at}}</a></li>
                </ul>
            </div>
            {{/each}}
        </div>
    </div>
</div>